pipeline {
    agent any

    stages {
        stage('clone') {
            steps {
                git branch: 'main', url: 'https://github.com/vishal0223/jenkins.git'
            }
        }
        stage('init') {
            steps {
                sh 'terraform init'
            }
        }
    }
}